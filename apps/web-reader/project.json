{
  "name": "web-reader",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/web-reader",
  "projectType": "application",
  "tags": [],
  "// targets": "to see all targets run: nx show project web-reader --web",
  "targets": {
    "copy-assets": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cp -r libs/design-system/src/lib/assets/* apps/web-reader/public/"
      }
    },
    "dev": {
      "dependsOn": ["copy-assets"]
    },
    "build": {
      "dependsOn": ["copy-assets"]
    }
  }
}
